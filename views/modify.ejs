<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/style3.css">
  <title>Edit Blog</title>
  
</head>

<body>
  <div class="container">
    <h1 class="heading">
      <%= heading %>
    </h1>

    <% if (locals.post) { %>
      <form id="editForm" method="post" action="/api/posts/<%=post.id%>">
        <input type="text" name="title" value="<%=post.title %>"><br>
        <textarea name="content" rows="10"><%=post.content %></textarea><br>
        <input type="text" name="author" value="<%=post.author %>"><br>
        <button class="full-width" type="submit">
          <%= submit %>
        </button>
      </form>
    <% } else { %>
      <form id="newPostForm" method="post" action="/api/posts">
        <input type="text" name="title" placeholder="Title" required><br>
        <textarea name="content" placeholder="Content" required rows="10"></textarea><br>
        <input type="text" name="author" placeholder="Author" required><br>
        <button class="full-width" type="submit">
          <%= submit %>
        </button>
      </form>
    <% } %>
  </div>
</body>

</html>
